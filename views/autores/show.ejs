<h2 class="page-header"><%= author.name %></h2>
<div class="btn-row">
  <a class="btn btn-primary" href="/autores/<%= author.id %>/edit">Editar</a>
  <%- include('../partials/deleteForm',
              { url: `/autores/${author.id}` }) %>
</div>              
<% if (booksByAuthor.length > 0) {%>
    <h2 class="page-header">Livros do Autor</h2>
    <%- include('../partials/bookGrid', { books: booksByAuthor }) %> 
<% } %>             